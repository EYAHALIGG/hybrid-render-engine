#pragma once

/*#include <unordered_map>

class Resource;

class ResourceManager
{
	static std::unordered_map<const char*, Resource*> resources;
public:
	template <class T> static void LoadResource(const char* name, const char* fileName);
};
*/
/*
ResourceManager* resourceManager = new ...;
resourceManager->LoadResource("car...",);
OBJModel* car = resouceManager->GetResource<OBJModel>("car");
*/